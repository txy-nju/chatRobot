services:
  chatrobot:
    build: .
    container_name: chatrobot
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./skills:/app/skills
    restart: unless-stopped
